{
  "name": "anchor-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor Android wrapper for the Anchor recovery companion app",
  "scripts": {
    "sync": "node sync-web.js",
    "cap:sync": "npm run sync && npx cap sync android",
    "android": "npm run cap:sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/local-notifications": "^6.0.0",
    "capacitor-native-biometric": "^4.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
